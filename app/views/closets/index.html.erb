<h1>Closets</h1>

<% @closets.each do |closet| %>
  <p><%= closet.name %></p>
  <p><%= image_tag closet.image_url %></p>
  <%= link_to "[New]", new_closet_path(@closet.id) %>
  <%= link_to "[Edit]", edit_closet_path(@closet.id) %>
  <%= link_to "[Delete]", dinosaur_path(@dinosaur.id), method: :delete,
  data: { confirm: "Are you sure?" } %>
<% end %>
