<h3>New Closet</h3>
<%= form_for @closet do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :image_url %>
  <%= f.text_field :image_url %>

  <%= f.submit (@closet.new_record? ? "Create" : "Update") %>

<% end %>
